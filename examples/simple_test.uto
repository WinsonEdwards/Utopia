@lang main {
    function add(a: number, b: number) -> number {
        return a + b;
    }
    
    function main() {
        let x = 5;
        let y = 10;
        let result = add(x, y);
        print(result);
    }
} 