// Working Multi-Language Demo
println("🎯 Utopia Working Demo")
println("=====================")

@lang python {
    def add_numbers(a, b):
        return a + b
    
    def greet_user(name):
        return "Hello " + name
}

@lang javascript {
    function multiply(x, y) {
        return x * y;
    }
    
    function say_hello(person) {
        return "Hi " + person + "!";
    }
}

@lang c {
    int subtract(int a, int b) {
        return a - b;
    }
    
    int square(int n) {
        return n * n;
    }
}

println("📊 Multi-language functions defined:")
println("🐍 Python: add_numbers, greet_user")
println("🌐 JavaScript: multiply, say_hello")  
println("⚡ C: subtract, square")
println("")

let x = 10
let y = 5
println("Sample values: x =", x, ", y =", y)
println("")

println("📝 Function signatures:")
println("Python: add_numbers(10, 5) → 15")
println("JavaScript: multiply(10, 5) → 50")
println("C: subtract(10, 5) → 5")
println("")

println("✅ Multi-language compilation successful!")
println("🚀 All language blocks ready for execution") 