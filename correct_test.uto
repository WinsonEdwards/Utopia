@lang utopia {
    let x = 5;
    let y = 10;
    let message = "Hello from Utopia Runtime!";

    println("🚀 Utopia Runtime Test");
    println("x = ", x);
    println("y = ", y);
    println("message = ", message);

    let result = x + y;
    println("x + y = ", result);

    if (x < y) {
        println("x is less than y");
    } else {
        println("x is greater than or equal to y");
    }

    let i = 0;
    while (i < 3) {
        println("Loop iteration: ", i);
        i = i + 1;
    }

    println("✅ Test completed successfully!");
} 