-- Generated by Utopia Compiler - SQL Backend
-- Multi-language support with runtime integration

-- Database schema and procedures
-- Note: Adapt connection strings and syntax for your specific RDBMS

-- Main execution block
BEGIN
    SELECT println('Hello from Utopia!') INTO result_value FROM DUAL;
    SELECT println('Multi-language compiler test') INTO result_value FROM DUAL;
    x := 42;
    message := 'Testing all languages';
    SELECT println('Integer:', x) INTO result_value FROM DUAL;
    SELECT println('String:', message) INTO result_value FROM DUAL;
    sum := (x + 10);
    SELECT println('Sum:', sum) INTO result_value FROM DUAL;
    SELECT println('Test completed successfully!') INTO result_value FROM DUAL;
END;
/

