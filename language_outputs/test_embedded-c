// Generated by Utopia Compiler - Embedded C Backend
// Optimized for microcontrollers and IoT devices

#include <stdint.h>
#include <stdbool.h>
#include <string.h>
#ifdef ARDUINO
#include <Arduino.h>
#endif

typedef union {
    int32_t i;
    float f;
    bool b;
    char* s;
} utopia_value_t;

typedef struct {
    utopia_value_t value;
    uint8_t type; // 0=int, 1=float, 2=bool, 3=string
} utopia_typed_value_t;

int main(void) {
#ifdef ARDUINO
    setup();
    while(1) {
        loop();
    }
#else
    // Embedded main loop
    while(1) {
        // Your embedded code here
    }
#endif
    return 0;
}
